package com.example.saborchef.model

/**
 *  El enum debe coincidir con tu clase Java `Rol` en el backend.
 *  Si en el backend tu Rol tiene valores: VISITANTE, ALUMNO, USUARIO, ADMIN,
 *  asegúrate de que aquí coincidan exactamente (mayúsculas, mismos nombres).
 */
enum class Rol {
    VISITANTE,
    ALUMNO,
    USUARIO,
    ADMIN
}
